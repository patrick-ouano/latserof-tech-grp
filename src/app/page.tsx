import { PageStub } from "@/components/PageStub";

/* The homepage is the one fully designed page — Direction 2a, "Gallery
   Black". Six bands: header, hero, "What we build", "Recent installations",
   gold CTA, footer. Build it against the handoff README; the copy there is
   final and must not be paraphrased. */
export default function HomePage() {
  return (
    <PageStub
      eyebrow="Orlando · Central Florida"
      title="Homepage"
      note="Designed and approved (Direction 2a, “Gallery Black”) — implementation is the next task."
    />
  );
}
